<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        
        <item name="android:windowBackground">@color/moja_barva</item> 
        
        <item name="android:statusBarColor">@color/moja_barva</item>
        <item name="android:navigationBarColor">@color/moja_barva</item>

        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <item name="android:windowFullscreen">false</item>
    </style>

    <style name="Theme.LaunchScreen" parent="Theme.SplashScreen">

        <item name="windowSplashScreenBackground">@color/moja_barva</item> 
        
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>

        <item name="postSplashScreenTheme">@style/AppTheme</item>

        <item name="android:statusBarColor">@color/moja_barva</item> 
        <item name="android:navigationBarColor">@color/moja_barva</item> 
    </style>

</resources>

